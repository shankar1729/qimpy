"""Developer harnesses for transport: multi-rank validation and bound checks.

Not imported by the package -- run directly, e.g.
``python -m qimpy.transport.tools.rank_sweep --mesh ... --out ...``.
They live here rather than in a top-level ``tools/`` so that everything
transport-specific stays inside the transport tree.
"""
